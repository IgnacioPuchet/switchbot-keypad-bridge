esphome:
  name: switchbot-keypad-bridge

esp32:
  variant: esp32s3
  framework:
    type: esp-idf

external_components:
  - source: github://pierluigizagaria/switchbot-keypad-bridge
    components: [switchbot_keypad_bridge]

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

logger:

api:

ota:
  - platform: esphome
    password: !secret ota_password

switchbot_keypad_bridge:
  pairing_ui: true

  keypad_action:
    name: "Action"

  keypad:
    name: "Keypad"

  unpair_button:
    name: "Unpair"

  # on_lock:
  #   - logger.log: "Locked"

  on_unlock:
    # method: "pin" | "fingerprint" | "face" | "unknown"   index: credential slot
    - homeassistant.event:
        event: esphome.switchbot_keypad_unlock
        data:
          method: !lambda 'return method;'
          index: !lambda 'return to_string(index);'
